{{ attach_library('the/bg-image') }}
{%
  set classes = [
    'node',
    'node--type-' ~ node.bundle|clean_class,
    node.isPromoted() ? 'node--promoted',
    node.isSticky() ? 'node--sticky',
    not node.isPublished() ? 'node--unpublished',
    view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
    'bg-image'
  ]
%}
{{ attach_library('the/node') }}
{% set link = content.field_link[0]['#url'] ? content.field_link[0]['#url'] : null %}
<article{{ attributes.addClass(classes) }}>
  {% if link %}
    <a href="{{ link }}">
   {% endif %}
    <div{{ content_attributes.addClass('box-display') }}>
      <div{{ create_attribute().addClass('box-display--inner') }}>
        {{ content|without(content.field_image, content.field_link) }}
      </div>
    </div>
    {{ content.field_image }}
  {% if link %}
    </a>
  {% endif %}
</article>
