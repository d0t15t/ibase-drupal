@require 'vars'
@require 'layouts/split-page'
//@require 'elements/bg-image'

.layout-container
  max-width $max_width

  //@media (min-width: $large-screen-breakpoint)
  //  margin: 50px 0 75px 50px;

  header
    //margin-bottom 50px

  .region-primary-menu
    //display flex
    //align-items center

  main
    @extend .split-page-wrapper

    .layout-content
      @extend .split-page-wrapper--content

    > aside
      @extend .split-page-wrapper--sidebar

.path-frontpage
  .layout-container
    margin 0
    max-width unset
    display: flex;
    flex-direction: column;
    height: 100vh;
    overflow: hidden;

    @media (min-width: $mobile-breakpoint)
      height auto
      display block

    header
      padding $spacing_layout--sm

      @media (min-width: $mobile-breakpoint)
        max-width: $max_width;
        padding $spacing_layout--md

      //padding-left: 50px;
      //max-width: $max_width;

    main
      flex: 1;
      max-width unset

      @media (min-width: $mobile-breakpoint)
        width 100%
      //display: flex;
      //flex-direction: column;
      //overflow: hidden; /* Prevent overflow */