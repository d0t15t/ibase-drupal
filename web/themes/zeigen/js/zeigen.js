/**
 * @file
 * Zeigen behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.zeigen = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
